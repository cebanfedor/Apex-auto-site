# AUDIT — Apex Auto / Feduk USA

## СТАТУС ИСПРАВЛЕНИЙ (итог 2026-09-01)
Все группы P0→P3 выполнены атомарными коммитами, `node --check` + `npm test` (13/13) зелёные,
`npm audit` = 0, каждое изменение проверено на проде.

| # | Статус | Коммит/примечание |
|---|---|---|
| P0-1 лиды главной/контактов | ✅ исправлено | POST на сервер + Telegram-доп; регрессия contacts.html восстановлена (`contacts-form.js`) |
| P1-1 bid-advice DoS/инъекция | ✅ исправлено | rate-limit, лимит тела, анти-инъекция, без утечки |
| P1-2 сырое «Failed to fetch» | ✅ исправлено | дружелюбный fallback |
| P2-1 деление на ноль (курс) | ✅ исправлено | `rateOf()` |
| P2-2 отрицат./огромная цена | ✅ исправлено | `num()` guard + `interpolateFee` guard в ядре |
| P2-3 телефон-мусор | ✅ исправлено | `isValidContact` (≥8 цифр или Telegram) |
| P2-4 VIN 17/без I,O,Q | ✅ исправлено | клиент (openVinReport) + сервер (fetchVin) |
| P2-5 w8-tracking лимит/утечка | ✅ исправлено | rate-limit 20/10мин, без detail/status |
| P2-6 XSS hot.js | ✅ исправлено | `esc()` на все поля |
| P2-7 XSS tracking.html | ✅ исправлено | `esc()` на все поля/URL |
| P2-8 тач-таргет шапки | ✅ исправлено | 44px на мобиле |
| P2-9 leads endpoint | ✅ задокументировано | комментарий в api/leads.js |
| P3-1 двойная отправка | ✅ исправлено | кнопка disabled до автозакрытия |
| P3-2 утечка ошибок | ✅ частично | публичные роуты (bid-advice, w8, uploads) — убрано; CRUD/content за админом — оставлено (низкий риск) |
| P3-3 admin login | ✅ исправлено | rate-limit 5/10мин + HMAC-сравнение пароля |
| P3-4 uploads (SVG/тип/размер) | ✅ исправлено | magic-байты, SVG запрещён, лимит 8МБ |
| P3-5 content whitelist | ✅ исправлено | по ключам DEFAULT_CONTENT |
| P3-7 .env в .vercelignore | ✅ исправлено | + AUDIT.md исключён из деплоя |
| P3-8 http body destroy | ✅ исправлено | `request.destroy()` |
| P3-9 undici | ✅ исправлено | `npm audit fix` → 0 |
| P3-10 .env.example | ✅ создан | — |
| P3-11 README.md | ✅ создан | — |
| P3-12 sitemap lastmod | ✅ исправлено | 2026-09-01 |
| P3-13 og:image cases/privacy | ✅ исправлено | + OG/twitter теги |
| P3-14 label for= | ✅ исправлено | 10 подписей калькулятора |
| Тесты (node:test) | ✅ добавлены | `npm test` 13/13; E2E — `test/E2E-SCENARIOS.md` |

### Довыполнено позже
- **P3-6 CORS** — ✅ сужен с `*` до allowlist (apexauto.md + localhost); расширение через host_permissions не затронуто.
- **P3-16 словарь damages** — ✅ статический снимок (server/usa-damages.json), 4с → ~0.3с cold.
- **P3-17 CSP `script-src 'unsafe-inline'`** — ✅ снято. Инлайн-обработчики → делегированный img-fallback.js; 14 инлайн-скриптов покрыты sha256-хешами. Проверено: GA, язык, калькулятор, фолбэк картинок работают, 0 CSP-нарушений. ⚠️ при правке любого инлайн-`<script>` пересчитывать его sha256 в vercel.json.

### НЕ исправлено (требует твоего решения)
- **P3-15 Шрифт Manrope vs Onest/Inter** — визуальное решение, редизайн не делаю. Жду команды: вернуть Manrope или обновить доки.
- **Формулы тарифов** (`calc-core.js`/`script.js` ставки/пошлины/акцизы) — по твоему указанию не трогал; математических ошибок в формулах не найдено (P2-1/P2-2 и guard ядра — это санитизация ВВОДА, не изменение тарифов).

---


**Дата:** 2026-09-01 · **Ветка:** main · **Репо:** cebanfedor/Apex-auto-site · **Прод:** Vercel → apexauto.md

## Важно про стек (расхождение с ТЗ)
ТЗ написано под **Next.js / React / TypeScript / Tailwind / Zod / Vitest**. Фактический стек —
**ванильный статический сайт (плоские `*.html` + `styles.css`/`auctions.css` + `script.js`/`auctions.js`/
`i18n.js`/…) + serverless-функции Vercel (`api/*.js`) + Supabase (`server/*.js`) + админ-CRM (`admin/`)**.
Ни Next.js, ни React, ни TS, ни Tailwind, ни Zod, ни Vitest в проекте нет (`package.json.scripts = {}`).
Аудит адаптирован под реальный стек: команды `npm run build/test/lint`, `tsc` заменены на
`node --check`, `npm audit`, проверку роутов и заголовков прода. Тесты — план на `node:test` (0 зависимостей).

## Фаза 0 — результаты прогонов
| Проверка | Результат |
|---|---|
| `node --check` по всем `.js` | ✅ синтаксис валиден везде |
| `npm audit` | 1 moderate: undici (транзитивная, `npm audit fix`) |
| Секреты в git | ✅ не найдены; `.gitignore` закрывает `.env*` |
| `.env.example` | ❌ отсутствует (deliverable ТЗ) |
| `README.md` | ❌ отсутствует (есть только `ADMIN_README.md`) |
| Заголовки прода | ✅ CSP, HSTS, X-Frame, nosniff, referrer применяются; кэш статики immutable/SWR (пофикшено этой сессией) |
| robots.txt / sitemap.xml | ✅ 200 |

## Уже исправлено в этой сессии (НЕ чинить повторно)
- **perf:** снят мёртвый `locations.js`+`script.js` с about/contacts/hot; `defer` на скриптах главной; ленивая
  загрузка словарей фильтров каталога; `updateTabCounts`/`updateCardForecasts` отложены на idle; hero/hot-фото → WebP;
  `Cache-Control` immutable для `.js/.css`, 30д+SWR для картинок (проверено на проде).
- **i18n:** счётчики каталога и AI-советчик ставки (`script.js`) переведены на RO/EN.
- **a11y:** тач-таргеты лайтбокса → 44px; `aria-label` инпутам лид-формы главной.
- **api:** `per_page` фолбэка 50→1000; честное окно годов в рыночной статистике.

---

## Находки по severity

### P0 — теряются деньги / ломает ключевой сценарий
| # | Файл:строка | Проблема | Как проявляется у пользователя | Предлагаемая правка |
|---|---|---|---|---|
| P0-1 | `script.js:1121,1146,1222` (`selectionForm`,`contactQuickForm`,`checkForm`); `openTelegramMessage` `:1115` | Формы главной (подбор, быстрый контакт, бесплатная проверка VIN) **не пишут лид на бэкенд** — только `window.open` Telegram-share. «✓ мы свяжемся» показывается вслепую | Если `window.open` заблокирован / нет Telegram / юзер не дожал «отправить» — заявка **теряется**, а клиент уверен, что оставил её. `contactQuickForm` вообще не даёт подтверждения | Слать эти формы на серверный `POST /api/auctions?action=lead` (Supabase+Telegram-бот, как форма аукционов), Telegram-share оставить доп. шагом. Успех показывать только после `res.ok` |

### P1 — реальный ущерб
| # | Файл:строка | Проблема | Как проявляется / риск | Предлагаемая правка |
|---|---|---|---|---|
| P1-1 | `api/bid-advice.js:171-187,239-264,298-327` | Публичный эндпоинт: нет авторизации, нет rate-limit, тело целиком уходит в промпт OpenAI с `web_search_preview` | Бот в цикле выжигает платный бюджет OpenAI (денежный DoS); prompt-injection через JSON тела влияет на вывод, рендерящийся в UI | Rate-limit по IP (переиспользовать `checkLeadRate`), лимит размера тела, обрамить пользовательские данные как недоверенный блок; при отсутствии `OPENAI_API_KEY` не звать |
| P1-2 | `auctions.js:288-298` (`api()`), `2257-2259` (`submitLead`) | Сетевой сбой заявки показывает сырое `Failed to fetch`/`Load failed` | При обрыве сети клиент видит английский тех-текст вместо «напишите в Telegram» — теряет доверие на самом важном шаге | Различать сетевую (нет `response`) и серверную ошибку; для сетевой — дружелюбный fallback с CTA в Telegram |

### P2 — заметно вредит
| # | Файл:строка | Проблема | Как проявляется / риск | Предлагаемая правка |
|---|---|---|---|---|
| P2-1 | `script.js:9,31` (`num`,`mdlToUsd/Eur`); поля `index.html:252-253` | Деление на ноль: если стереть курс USD/EUR → `total = total/0` | `#total`/`#subTotal` показывают `$∞` или `$NaN` — расчёт выглядит сломанным | Клемпить `usdMdl/eurMdl` к `>0` (дефолт) перед делением. **Не формула тарифа — это guard ввода** |
| P2-2 | `index.html:205` (`lotPrice`), `script.js:961` | `min="0"` не мешает ввести `-5000` вручную; `1e999`→`Infinity` | Отрицательный или `$∞` итог «под ключ» | `num("lotPrice")` → `Math.max(0, min(разумный max, …))`; так же market/repair. **Guard ввода, не формула** |
| P2-3 | `api/auctions.js:1206-1215` | Телефон валидируется только длиной ≥5; мусор `aaaaa`/`00000` создаёт лид+клиента (upsert по phone) | Грязная база, дубли-мусор, обзвон впустую | Нормализация (только `+` и цифры), минимум 8–9 цифр, опц. whitelist `+373/+40/+7`; на фронте `pattern`+подсказка |
| P2-4 | `auctions.js:302-309` (`parseSmartSearch`), `api/auctions.js:1041-1046` (`fetchVin`) | VIN на «боевом» пути принимает 11–16 символов и буквы I/O/Q; строгая маска есть только в `detectVin` (`script.js:611`) | 11–16-значный «VIN» уходит в запрос, юзер видит «лот не найден» вместо «VIN некорректен» — вводит в заблуждение | Применить маску `[A-HJ-NPR-Z0-9]{17}` (17 симв., без I/O/Q) в `parseSmartSearch`/`fetchVin`; явное сообщение об ошибке VIN |
| P2-5 | `api/w8-tracking.js:1-24` (нет лимита), `:20,23` (утечка `status`/`e.message`) | Публичный VIN-трекинг без rate-limit; наружу уходит upstream-статус и `e.message` | Усилитель нагрузки/перебор VIN; мелкая утечка инфраструктуры | Rate-limit по IP; клиенту — обобщённый текст, детали в серверный лог; опц. кэш отрицательных ответов |
| P2-6 | `hot.js:22,24,28,38,45,163,174,186,187` | **XSS (DOM):** `lot.title/damage/fuel/drive/description/imgSrc` в `innerHTML` без экранирования | HTML/`<img onerror>` в полях лота (из стороннего API или из `vehicles.description` в админке) исполнится у посетителя | Локальный `escapeHtml` (как в `script.js:28`) на все интерполяции, включая `src`/`alt` |
| P2-7 | `tracking.html:801,903,915,924,962,986-993` | **XSS (DOM):** скрейпленные данные W8 (`d.vehicle/lotNumber/auction/city`, фото-URL) в `innerHTML` без экранирования | HTML в ответе upstream `w8shipping.ua` исполнится; фото-URL без валидации схемы | Экранировать все поля; фото-URL — проверять префикс `https://static.w8shipping.com/` (в API regex уже есть) |
| P2-8 | `styles.css:800,1211` (`.headerPhoneV308` 34–36px) | Тач-таргет телефона в шапке <44px на мобиле (лайтбокс уже пофикшен) | Мелкая цель на 90% мобильного трафика | Поднять до ≥40–44px через padding/min-height, не ломая высоту шапки |
| P2-9 | `api/leads.js:24`, `server/crud.js:45` | «Канонический» `POST /api/leads` закрыт `requireAdmin` — публичный лид туда невозможен (фронт обходит через `?action=lead`) | Мина для сопровождающего: будущая интеграция на `/api/leads` получит 401 | Задокументировать, что публичный create — только `handleLead`; либо развести админ-CRUD и публичный create |

### P3 — техдолг / упрочнение / косметика
| # | Файл:строка | Проблема | Правка |
|---|---|---|---|
| P3-1 | `auctions.js:2247-2262` | Двойная отправка в окне 2.2с шлёт пустой запрос → 400 поверх «успеха» | Не переоткрывать кнопку в `finally` при успехе (держать disabled до закрытия модалки) |
| P3-2 | `server/crud.js:70,100`; `api/admin.js:86`; `api/content.js:177`; `api/bid-advice.js:329` | Внутренние сообщения/детали БД в JSON (за админом — низкий риск; публично `bid-advice`/`w8` — выше) | Не возвращать `details`/`error.message` на публичных роутах |
| P3-3 | `api/admin.js:24-42`; `server/auth.js:16-21` | Нет rate-limit на login (брутфорс, 1.5с/попытку); ранний выход по длине пароля (утечка длины по таймингу) | Лок по IP на `action=login`; хешировать обе стороны до `timingSafeEqual` |
| P3-4 | `api/uploads.js:4-9,36-40,29-30` | Тип по клиентскому заголовку; разрешён `svg` (хранимый XSS); нет лимита размера | Проверять магические байты; `svg` запретить или отдавать `attachment`; лимит размера. (За `requireAdmin`) |
| P3-5 | `api/content.js:162` | Нет whitelist полей контента (`{...body}`) | `cleanPayload(body, ALLOWED_CONTENT_FIELDS)` как в `crud.js:5` |
| P3-6 | `server/calc-handler.js:213-215` | CORS `*` (для Chrome-расширения; секретов/мутаций нет — приемлемо) | Оставить или сузить до нужных origin |
| P3-7 | `.env.local`, `.env.debug` | Не внесены в `.vercelignore` (в git не попадают; Vercel по дефолту исключает — риск низкий) | Добавить `.env*` в `.vercelignore`; ротировать `VERCEL_OIDC_TOKEN` |
| P3-8 | `server/http.js:20-38` | Лимит тела 12MB не прерывает поток (`request.destroy()` нет) | Вызывать `request.destroy()` при превышении, как в `calc-handler.js:68` |
| P3-9 | `node_modules/undici` | `npm audit`: 1 moderate (cookie injection) | `npm audit fix` |
| P3-10 | корень | Нет `.env.example` (deliverable ТЗ) | Создать со всеми env-переменными без значений |
| P3-11 | корень | Нет `README.md` (deliverable ТЗ) | Локальный запуск, env, деплой |
| P3-12 | `sitemap.xml` | Часть `<lastmod>` = 2026-06-01 (стейл) | Обновить lastmod |
| P3-13 | `cases.html`, `privacy.html` | Нет `og:image` (соц-превью без картинки) | Добавить `og:image` |
| P3-14 | `index.html:144,154,171…` | Label калькулятора без `for=` (a11y) | Добавить `for=` к 21 label (id у контролов есть) |
| P3-16 | `api/auctions.js:1859` (`usadict=damages`) | Словарь повреждений тянет все страницы live ~4с при первом открытии фильтра | Забить статичным JSON в репозитории |
| P3-17 | `vercel.json:51` | CSP `script-src 'unsafe-inline'` ослабляет XSS-защиту (нужен для инлайн-gtag) | Архитектурный долг: вынести инлайн-скрипты и убрать `unsafe-inline` (крупная работа) |

---

## Требует ТВОЕГО решения (не правлю без подтверждения)
- **Формулы расчёта** (`script.js`, `calc-core.js`, `api/auctions.js` ставки/пошлины/акцизы) — по твоему указанию не трогаю. Если в аудите видна математическая проблема — она попадёт сюда отдельно (пока таких **не найдено**; P2-1/P2-2 — это guard'ы ввода, не формулы).
- **P3-15 Шрифт:** ТЗ и `CLAUDE.md` говорят «Manrope», но фактически грузятся **Onest + Inter** (`styles.css:1`, `@import`). Либо вернуть Manrope, либо обновить ТЗ/доки. Редизайн не делаю — жду решения.
- **Тесты:** по твоему выбору — лёгкий раннер `node:test` (0 зависимостей) для юнит-тестов калькулятора и валидаций; Playwright-сценарии — текстом.

---

## План тестов (по решению «node:test + Playwright текстом»)
**Юнит (`node:test`), обязательно краевые случаи:**
- Калькулятор: 0 / отрицательное / `1e999` в `lotPrice`; пустой курс USD/EUR (÷0); отсутствие локации/топлива; проверка отсутствия `NaN`/`Infinity` в `#total`.
- Валидация телефона: `+373`, `+40`, `+7`, мусор, пусто.
- VIN: ровно 17 символов; наличие I/O/Q; длина 11–16.
- Санитизация лид-полей (`handleLead`): honeypot, обрезка, rate-limit.

**E2E (Playwright, описание сценариев — happy + error):**
1. Калькулятор: ввод параметров → корректный итог; краевой ввод → нет `$NaN`/`$∞`.
2. Заявка: заполнить/отправить → подтверждение; обрыв сети → дружелюбная ошибка; двойной клик → один лид.
3. VIN: валидный 17-симв. → отчёт; невалидный → явное сообщение об ошибке VIN.

---

## Предлагаемый порядок правок (после подтверждения)
1. **P0-1** (лиды главной на бэкенд) — деньги.
2. **P1** (bid-advice лимит/инъекция; дружелюбная сетевая ошибка).
3. **P2** (÷0 и клемпы ввода; телефон; VIN-маска; XSS в hot.js/tracking.html; w8 rate-limit; тач-таргет шапки).
4. **P3** пакетами (утечки ошибок, admin login лимит, uploads, `.env.example`+`README`, sitemap, og:image, `for=`, undici).
5. После каждой группы — `node --check` всех затронутых файлов + ручная проверка на проде/локально + отдельный коммит `fix(scope): …`.

**Остановка. Подтверди объём и приоритеты — начну с P0.**
